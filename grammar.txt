expr: term ((PLUS|MINUS) term)*
    : var-expr

var-expr: expr IDENTIFIER EQ expr

type: IDENTIFIER

term: factor ((MUL|DIV) factor)*

atom: INT|FLOAT|IDENTIFIER
	: LPAREN expr RPAREN
    : (PLUS|MINUS) atom
