class Point {
    fn constructor(x, y) {
        this.x = x;
        this.y = y;
    }

    fn magnitude() {
        return Math.sqrt(this.x * this.x + this.y * this.y + this.z * this.z);
    }
}

////////////////////////////////////////
var vec = new Vector(0, 1, 0);
echo vec.magnitude();