class Point {
    fn constructor(x, y) {
        this.x = x;
        this.y = y;
    }

    fn plus(other) {
        return new Point(this.x + other.x, this.y + other.y);
    }
}

var x = new Point(1, 1);
var y = new Point(1, 2);

Console.println(x + y);